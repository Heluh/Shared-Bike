server:
  port: 8085

spring:
  application:
    name: fence-service # 服务名称
  profiles:
    active: dev # 激活的配置文件
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848 # Nacos地址
      config:
        file-extension: yaml # 文件后缀名
