spring:
#  application:
#    name: prediction-config
  cloud:
    nacos:
      config:
        server-addr: localhost:8848 #nacos中心地址
        file-extension: yaml # 配置文件格式

  profiles:
    active: dev # 环境标识
